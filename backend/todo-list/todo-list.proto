syntax = "proto3";

import "google/api/annotations.proto";

package todolist;

service TodoList {
	rpc AddTodo(TodoRequest) returns (TodoReply) {}
}

message TodoRequest {
	string name = 1;
}

message TodoReply {
	string name = 1;
}
